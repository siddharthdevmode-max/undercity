// ============================================================
// SKIP NAVIGATION LINK
// Invisible until focused via Tab
// Lets keyboard users skip past nav straight to main content
// ============================================================

export default function SkipNav() {
  return (
    <a href="#main-content" className="skip-nav">
      Skip to main content
    </a>
  );
}
